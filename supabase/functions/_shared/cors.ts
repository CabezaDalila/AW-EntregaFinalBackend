export const corsHeaders = {
  'Access-Control-Allow-Origin': '*', // Permitir todas las solicitudes (reemplaza '*' con un dominio específico si quieres restringirlo)
  'Access-Control-Allow-Methods': 'GET, POST, PUT, DELETE, OPTIONS', // Métodos HTTP permitidos
  'Access-Control-Allow-Headers': 'Content-Type, Authorization,x-api-key', // Encabezados que aceptas
 };